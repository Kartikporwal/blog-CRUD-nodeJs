<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
        <a class="bg-red-500 px-3 py-2 rounded-md mb-5 inline-block text-sm"href="/logout">Logout</a>
        </div> 
        <h5 class="mb-5">edit your post.</h5>
        <form method="post"action="/update/<%= post._id %>">
           <textarea placeholder="What's on your mind ?"class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block"name="content"><%= post.content %></textarea>
           <input class=" w-40 px-3 py-2 text-sm text-black bg-yellow-500 block rounded-md mt-2 "type="submit" value="Update Post">
        </form> 
    </div>
</body>
</html>